# Preprint: Distance-hereditary graphs characterize minimum-fusion synthesis of graph states

- `paper.tex` — LaTeX source; builds with `tectonic paper.tex` (no external `.bib`; bibliography inline).
- `paper.pdf` — compiled output.

Provenance: the scientific prose was authored/revised by Fable 5 through the harness client
(ledger run `paper-fable-review-v1` in `runs/p5-formalize`), with claims audited against a
ground-truth fact sheet drawn from the ledger artifacts. The results it reports are the P5
campaign artifacts: the 585-orbit tablebase (`dc8649…f25`, runs/p5-live), the Fable-generated
DH characterization (`85e7a63c…`), and the Lean formalization chain (foundation modules in
`lean/EmpiricistLean/`; leaf theorems as gate-certified artifacts in `runs/p5-formalize`).
See `docs/science/` for the underlying notes.
