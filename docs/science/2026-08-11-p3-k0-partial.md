# P3 (k=0): "at most 3 of 4 Bell states are identifiable" — formalization status (partial)

**Date:** 2026-08-11 · **Problem:** P3, k=0 (passive dual-rail, no ancilla) ·
**Method:** M20d Fable→Lean formalize loop (Fable authors all Lean; the hardened
gate verifies; the coordinator reviews faithfulness + promotes). Ledger
`runs/p3-campaign`.

## The result (mathematics: settled)

For every passive 4-mode ancilla-free dual-rail scheme (`U ∈ U(4)`), **at most
three of the four Bell states admit unambiguously-identifying detection patterns**,
so `min_B p_B = 0`. This is the Calsamiglia–Lütkenhaus k=0 ceiling, re-derived by
Fable as the L1–L4 proof sketch (ledger artifact `aed4942e`), whose checkable
pieces the harness engine-verified. **There is no open mathematical question and
no gap in the argument.** The `½` success-probability certificate for the standard
BSM is separately machine-verified on `main`.

## Formalization status

The theorem decomposes as (Fable's own structure):
`p3_at_most_three : ∀ U ∈ unitaryGroup (Fin 4) ℂ, ¬ ∀ μ, ∃ i j, Identifies U i j μ`,
proved by: bridge (Identifies ⟹ row-decoupling at both detectors) → L2 (each
detector row-decouples for ≤ 2 labels) → Counting (the covered configuration is
two-doubled-edges or a 4-cycle) → case_doubled (doubled ⟹ ≤ 2 identified,
contradiction) ∨ case_fourCycle (the 4-cycle dies on unitarity: L3/L4).

**Machine-verified through the hardened FORMALIZED gate and promoted to the
trusted foundation (all on `main`, all faithfulness-reviewed, axioms ⊆ {propext,
Classical.choice, Quot.sound}):**

| module | decl | content |
|---|---|---|
| P3Amplitudes | `rawAmp_eq_traceForm` | pattern/amplitude defs + the Pauli trace form of `Q` |
| P3Pauli | `pauli_no_common_eigenvector` | the 2×2 Pauli toolbox |
| P3L1 | `identifies_iff_prop` | `Identifies ⟺ Q ∝ σ_μ` |
| P3L2 | `card_Mset_le_two` | each detector row-decouples for ≤ 2 labels |
| P3Counting | `cover_structure` | doubled-edges ∨ 4-cycle dichotomy |
| P3Bridge | `identifies_rowDecouples` | the L1→L2 bridge |
| P3Doubled | `case_doubled` | the doubled-edges case ⟹ False |

The **assembly** `p3_at_most_three` + corollary `p3_min_support` is written by
Fable and is **complete given the two case lemmas** (see
`2026-08-11-p3-k0-partial/p3_main_assembly.skeleton.lean`).

## What remains: one lemma (`case_fourCycle`, the L4 crux)

The 4-cycle case is fully set up in Lean — the cyclic label-set structure, the
4-distinctness combinatorics (`four_distinct_aux`), and **unitary row-orthogonality**
(`unitary_row_orth_aux`, which gives `⟨row_i0, row_i2⟩ = 0` for the complementary
detectors). The single unclosed hole is the **L4 crux computation**: proving the
*same* inner product is **nonzero**,

> `⟨row_i0, row_i2⟩ = x·x† + λ·λ̄ · x·Q·x† ≠ 0`,

where `x·σ_y` is a shared eigenvector of a non-identity Pauli `P` and `Q`
anticommutes with `P` (hence is off-diagonal in `P`'s eigenbasis), by a case-split
on whether the two eigenvectors are parallel (first term ≠ 0, second = 0) or
orthogonal (first = 0, second ≠ 0). Contradicting `= 0` gives `False`.

**This is a formalization-hardness boundary, not a mathematical obstacle.** The
computation is a few lines of elementary 2×2 complex linear algebra on paper; in
Lean it is stubborn because mathlib's support for concrete 2×2 complex eigenbasis
reasoning is thin, so it must be done by hand in explicit `Fin 2 → ℂ` coordinates
with fiddly off-diagonal matrix-element manipulation. The formalize loop did not
close it across **22 rounds / 3 full runs / ~$23** of live Fable, including a
micro-decomposition of the crux into eigenvector / anticommuting-off-diagonal /
inner-product-expansion / case-split sub-steps. Recorded as a partial per the M20d
plan's explicit "partial success is still recorded value" clause. The partial
`case_fourCycle` (scaffolding + the one hole) is preserved at
`2026-08-11-p3-k0-partial/case_fourCycle.partial.lean` for resumption.

## L4 crux — progress + the precisely-characterized remaining machinery (2026-08-20)

Two more trusted-foundation modules were added toward L4:
- **P3TwoByTwo** (`axis_pairing_not_both_zero`): the abstract crux engine — for
  nonzero **axis-aligned** (σ_z-eigenvector) `u,v`, NOT both `plainPair`
  (`u₀v₀+u₁v₁`) and `twistPair` (`u₀v₁+u₁v₀`) vanish. Closed in 1 round once
  isolated (the prior $23 inline stall was purely non-isolation).
- **P3Eigvec** (`pauli_eigvec_axis_aligned`): a nonzero σ_z-eigenvector is
  axis-aligned.

`case_fourCycle` now imports and *uses* both, but does not yet close. The
connection revealed the exact remaining work, and a key subtlety:

**The engine is σ_z-specific and does NOT generalize.** For σ_x eigenvectors
`(a,a),(b,−b)`: `plainPair = twistPair = 0` — both vanish — so a naive
"general-Pauli pairing" lemma is **provably false**. The physics vectors
`xRow·σ_y` are eigenvectors of a *general* Pauli `P = pauliOf a · pauliOf b`
(`pauliOf` maps the 4 Bell labels to `{I, σ_x, σ_y, σ_z}`), so closing L4 needs the
full **basis-change machinery**:
1. **Per-Pauli diagonalizers** `B_μ` with `B_μ (pauliOf μ) B_μ⁻¹ = σ_z`, mapping
   each non-identity Pauli's eigenvectors to axis-aligned. Verified explicit
   matrices: `B_{σx} = !![1,1;1,-1]`, `B_{σy} = !![1,-I;1,I]` (both conjugate to
   σ_z AND axis-align; `B_{σz} = 1`). Extend P3Eigvec with these (fin_cases over
   BellLabel).
2. **Inner-product transform**: `⟨u,v⟩` relates to `plainPair(B_μ u, conj(B_μ v))`
   via `B_μ† B_μ = 2·1` (so `⟨u,v⟩ = ½⟨B_μ u, B_μ v⟩`). Also note the unitary inner
   product is **sesquilinear** (`U * star U` conjugates the second row) while
   `plainPair/twistPair` are bilinear — apply the engine to `conj(x₂·σ_y)`
   (`AxisAligned v ⟺ AxisAligned (conj v)`).
3. Then `case_fourCycle`: `fin_cases` on the labels → concrete `P` per branch →
   diagonalize → engine ⟹ `⟨row_i0,row_i2⟩ ≠ 0`, contradicting unitarity's `= 0`.

Then the assembly `P3Main` (importing the case lemmas) → `p3_at_most_three`
FORMALIZED. **Status:** engine + σ_z reduction done; the basis-change machinery
(step 1–2, ~2 modules) + the connection + assembly remain. This is
formalization-labor on a fully-understood, sound argument — an error-prone
2×2-complex-eigenbasis layer that mathlib does not streamline (two draft statements
were caught wrong before spending, and one vacuous `: True` result was caught in
faithfulness review and rejected — the review discipline working as designed). P3
formalize spend to date ≈ $130.
